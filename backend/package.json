{
  "name": "nidar-airmouse-backend",
  "version": "1.0.0",
  "description": "Backend API for the NIDAR AirMouse mission dashboard",
  "main": "server.js",
  "type": "commonjs",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "NODE_ENV=test node --test tests/*.test.js",
    "create-admin": "node scripts/create-admin.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "mongoose": "^8.5.0",
    "nodemailer": "^10.0.0"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0"
  }
}
